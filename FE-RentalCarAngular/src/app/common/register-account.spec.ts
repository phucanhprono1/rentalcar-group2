import { RegisterAccount } from './register-account';

describe('RegisterAccount', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new RegisterAccount()).toBeTruthy();
  });
});
